<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.secondzip.backend.record.mapper.ChecklistAnalysisResultMapper">

    <insert id="upsertResult">

        INSERT INTO recording_checklist_results (
            recording_session_id,
            checklist_item_id,
            status,
            confidence_score,
            evidence_text,
            reason
        )
        VALUES (
                   #{recordingSessionId},
                   #{item.checklistItemId},
                   #{item.status},
                   #{item.confidenceScore},
                   #{item.evidenceText},
                   #{item.reason}
               )

            ON DUPLICATE KEY UPDATE
                                 status = VALUES(status),
                                 confidence_score = VALUES(confidence_score),
                                 evidence_text = VALUES(evidence_text),
                                 reason = VALUES(reason),
                                 modified_at = CURRENT_TIMESTAMP

    </insert>

    <delete id="deleteByRecordingSessionId">

        DELETE
        FROM recording_checklist_results
        WHERE recording_session_id = #{recordingSessionId}

    </delete>

</mapper>